import React from 'react'
import AudioButton from './AudioButton'

export default {
  title: 'Components/Audio Button',
  component: AudioButton,
}

const Template = (args) => <AudioButton {...args} />

export const Primary = Template.bind({})
